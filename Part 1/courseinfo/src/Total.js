 let tot=0;
const Total=({parts})=>{

      parts.map(
          part=>{tot+=part.exercises
            return tot}
             
         )
    

    const total=tot;

    return (<p>Number of exercises {total}</p>)
}
export default Total;